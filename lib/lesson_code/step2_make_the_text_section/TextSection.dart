import 'package:flutter/material.dart';

class TextSection extends StatelessWidget {

  const TextSection();

  @override
  Widget build(BuildContext context) {

    // TODO 1) Return a Container with 32 padding on all sides.
    return Container(
      // Start with this Container
    );
    // TODO 2) Give it a Text for a child. Set the text string to AppStrings.bodyText and softwrap to true.

  }
}