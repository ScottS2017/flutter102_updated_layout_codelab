import 'package:flutter/material.dart';

class TitleSelection extends StatelessWidget {

  const TitleSelection();

  /// This is going to show the name of the place being featured, it's location and
  /// rating (in stars).
  @override
  Widget build(BuildContext context) {
return Container(
  // Start with this Container
);
    // TODO 1) Return a Container with 32 padding in all directions

    // TODO 2) Create a Row with main axis size set to minimum.

    // TODO 3) In the Row, create an Expanded with a Column in it. Set the cross axis alignment to start, main to min.

    // TODO 4) In it, place a Container with bottom padding of 8.

    // TODO 5) In that Container, place a Text Widget. Make it bold and use AppStrings.oeschinenLake for the string.

    // TODO 6) Below the Container add another Text. String = AppStrings.kandersteg and set the color to grey[500].

    // TODO 7) Below the Expanded, add a start icon and set its color to red[500].

    // TODO 8) Last, make a Text and set its string to 41. We're not going to bother stying this one.
  }
}